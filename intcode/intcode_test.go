package intcode

import "testing"

func TestIntCode(t *testing.T) {
	tests := []struct {
		name      string
		inputFile string ``
	}{
		// TODO: test cases
	}
	for _, test := range tests {
		t.Run(test.name, func(t *testing.T) {

		})
	}
}
